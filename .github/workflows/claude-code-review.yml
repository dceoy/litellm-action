---
name: Pull request review using Claude Code with LiteLLM proxy
on:
  workflow_call:
    inputs:
      claude-args:
        required: false
        type: string
        description: Additional arguments to pass directly to Claude Code Action
        default: '--allowedTools "mcp__github_inline_comment__create_inline_comment"'
      runs-on:
        required: false
        type: string
        description: Runner to use
        default: ubuntu-latest
    secrets:
      GEMINI_API_KEY:
        required: false
        description: API key for Google Gemini
      GH_TOKEN:
        required: false
        description: GitHub token for accessing the repository
permissions:
  contents: read
  pull-requests: write
  issues: write
  id-token: write
  actions: read
jobs:
  claude-code-review:
    if: >
      github.event_name == 'pull_request'
    runs-on: ${{ inputs.runs-on }}
    env:
      CLAUDE_CODE_ACTION: dceoy/claude-code-action@main
      LITELLM_MASTER_KEY: sk-litellm
    steps:
      - name: Checkout repository
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6.0.2
        with:
          fetch-depth: 1
          token: ${{ secrets.GH_TOKEN || secrets.GITHUB_TOKEN }}
          persist-credentials: false
      - name: Set environment variables
        run: >
          {
            claude_code_action_ref="${CLAUDE_CODE_ACTION##*@}";
            echo "CLAUDE_CODE_ACTION_REPOSITORY=${CLAUDE_CODE_ACTION%@*}";
            echo "CLAUDE_CODE_ACTION_REF=${claude_code_action_ref}";
            if [[ "${claude_code_action_ref}" =~ ^[0-9a-f]{40}$ ]]; then
              echo "CLAUDE_CODE_ACTION_IS_IMMUTABLE=true";
            else
              echo "CLAUDE_CODE_ACTION_IS_IMMUTABLE=false";
            fi;
            echo "CLAUDE_CODE_ACTION_CACHE_DIR=${RUNNER_TEMP}/claude-code-action-cache";
          } >> "${GITHUB_ENV}"
      - name: Cache Claude Code Action assets
        if: env.CLAUDE_CODE_ACTION_IS_IMMUTABLE == 'true'
        id: claude-code-action-cache
        uses: actions/cache@cdf6c1fa76f9f475f3d7449005a359c84ca0f306  # v5.0.3
        with:
          path: ${{ env.RUNNER_TEMP }}/claude-code-action-cache
          key: claude-code-action-${{ env.CLAUDE_CODE_ACTION_REF }}
      - name: Download Claude Code Action (if not cached)
        if: env.CLAUDE_CODE_ACTION_IS_IMMUTABLE != 'true' || (! steps.claude-code-action-cache.outputs.cache-hit)
        run: |
          mkdir -p "${CLAUDE_CODE_ACTION_CACHE_DIR}"
          curl -fsSL \
            -o "${CLAUDE_CODE_ACTION_CACHE_DIR}/${CLAUDE_CODE_ACTION_REF}.tar.gz" \
            "https://github.com/${CLAUDE_CODE_ACTION_REPOSITORY}/archive/${CLAUDE_CODE_ACTION_REF}.tar.gz"
      - name: Extract and install agents and commands from Claude Code Action
        run: |
          [[ -d .claude ]] || mkdir -p .claude
          tar -xzf "${CLAUDE_CODE_ACTION_CACHE_DIR}/${CLAUDE_CODE_ACTION_REF}.tar.gz" -C "${RUNNER_TEMP}/"
          rsync -av "${RUNNER_TEMP}/claude-code-action-${CLAUDE_CODE_ACTION_REF}/.claude/agents/" .claude/agents/
          rsync -av "${RUNNER_TEMP}/claude-code-action-${CLAUDE_CODE_ACTION_REF}/.claude/commands/" .claude/commands/
          [[ -f .claude/commands/review-pr.md ]] || exit 1
      - name: Start LiteLLM proxy server
        id: litellm-proxy
        uses: ./
        env:
          LITELLM_ANTHROPIC_DISABLE_NO_PROVIDER_ERROR: 'true'
          LITELLM_GEMINI_OPUS_MODEL: gemini/gemini-3.1-pro-preview
          LITELLM_GEMINI_SONNET_MODEL: gemini/gemini-3.1-pro-preview
          LITELLM_GEMINI_HAIKU_MODEL: gemini/gemini-3.1-pro-preview
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        with:
          api-key: os.environ/LITELLM_MASTER_KEY
          config: |
            model_list:
              - model_name: ll-opus
                litellm_params:
                  model: os.environ/LITELLM_GEMINI_OPUS_MODEL
                  api_key: os.environ/GEMINI_API_KEY
              - model_name: ll-sonnet
                litellm_params:
                  model: os.environ/LITELLM_GEMINI_SONNET_MODEL
                  api_key: os.environ/GEMINI_API_KEY
              - model_name: ll-haiku
                litellm_params:
                  model: os.environ/LITELLM_GEMINI_HAIKU_MODEL
                  api_key: os.environ/GEMINI_API_KEY
            litellm_settings:
              drop_params: true
              fallbacks:
                - ll-opus:
                    - ll-sonnet
                    - ll-haiku
                - ll-sonnet:
                    - ll-haiku
      - name: Run PR review with Claude Code
        uses: dceoy/claude-code-action@main  # zizmor: ignore[unpinned-uses]
        env:
          ANTHROPIC_BASE_URL: ${{ steps.litellm-proxy.outputs.base-url }}
          ANTHROPIC_DEFAULT_OPUS_MODEL: ll-opus
          ANTHROPIC_DEFAULT_SONNET_MODEL: ll-sonnet
          ANTHROPIC_DEFAULT_HAIKU_MODEL: ll-haiku
        with:
          claude_code_oauth_token: ''
          anthropic_auth_token: ${{ env.LITELLM_MASTER_KEY }}
          prompt: >-
            /review-pr REPO: ${{ github.repository }} PR_NUMBER: ${{ github.event.pull_request.number }}
          claude_args: ${{ inputs.claude-args }}
